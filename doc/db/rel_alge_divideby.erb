<%section '除法' do%>
% paragraph do
除法運算有兩個運算元：
第一個表格當作是「被除表」，
第二個表格當作是「除表」。
被除表的欄位必須要比除表多上一個以上，
令被除表欄位 num 為 [X1,X2...,Y1,Y2...]，
而除表欄位 den 為 [Y1,Y2...]，
則 num DIVIDEBY den 的欄位應為 [X1,X2...]，
而結果紀錄的 [X1,X2...] 為在原本的 num 中的具有 den [Y1,Y2...]。
% end
% paragraph do
除法可類比為乘法的還原運算，如下等式所示。
* num = quo TIMES    den
* quo = num DIVIDEBY den

% end
% paragraph do
通常除法會運用在找出符合 den 所有紀錄的語法，
如<%=xref 'divide_by_alge'%>的例子，
表示找出供應所有原料的供應商編號。
% end
% code '找出供應所有原料的供應商編號', 'divide_by_alge' do
suppliers_parts[s_id, p_id] DIVIDEBY parts[id as p_id]
% end
% paragraph do
下面是另外一個例子，employees 記錄員工資料，
skills 記錄技能資料，
<pre>employees_skills</pre> 記錄員工所會的技能，
則可用 DIVIDEBY 取得會所有技能的員工姓名。
% end
% code '找出會所有技能的員工姓名', 'emp_have_all_skills' do
(employees_skills[e_id, s_id] DIVIDEBY skills[id as s_id] 
JOIN employees)[name]
% end
<%end%>
